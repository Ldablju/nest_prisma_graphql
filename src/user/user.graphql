type User {
  id: Int!
  name: String
  location: Float
}

type Query {
  user(id: Int!): User
  findLove: [User!]!
}

type Mutation {
  removeUser: String!
  changePassword(password: String!): String!
}
